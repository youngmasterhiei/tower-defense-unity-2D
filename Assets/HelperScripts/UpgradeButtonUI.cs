using UnityEngine;
using TMPro;

public class UpgradeButtonUI : MonoBehaviour
{
    // Directly drag the child objects into these slots in the inspector once on the prefab
    public TMP_Text nameText;
    public TMP_Text levelText;
    public TMP_Text costText;
}
